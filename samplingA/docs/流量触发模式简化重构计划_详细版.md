# 流量触发模式彻底简化重构计划（详细版）

> 文档版本：v1.0
> 创建日期：2026-02-07
> 基于工程师方案整合，补充代码级实现细节

---

## 1. 背景与问题分析

### 1.1 当前问题现象

```
TSDB_W: type=6 len=12 OK, ts=1770460357
[UART7] 流量开始(60s确认): 16.25 >= 12.0
[流量通知] 检测到流量开始于 1770460357
[10:32:38][流量调度] ========== 检测到流量开始 ==========
[10:32:38][流量调度] 已在运行，忽略触发  ← 问题：流量恢复后未触发瞬时送样
```

### 1.2 根本原因分析

| 问题点 | 文件位置 | 具体原因 |
|--------|----------|----------|
| 流量恢复被忽略 | `Flowtrigger.c:351` | `is_running` 为真时直接返回，未区分流量恢复场景 |
| 停流处理位置错误 | `Flowtrigger.c:523` | 停流处理在 `cycle_started` 判断之后，等待整点阶段无法执行 |
| 通知被覆盖 | `sampling.c:2664` | `0xFF` 停流通知被桶ID覆盖（单值语义） |
| task4单值通知 | `freertos_app.c:1461` | `ulTaskNotifyTake` 不支持多事件并存 |

### 1.3 现有代码问题代码段

**问题1：流量恢复被忽略** (`Flowtrigger.c:350-356`)
```c
// 检查是否已在运行
if (g_ft_scheduler.is_running) {
    rtc_time_get();
    printf("[%02d:%02d:%02d][流量调度] 已在运行，忽略触发\r\n",
           calendar.hour, calendar.min, calendar.sec);
    return;  // ★ 问题：未区分是正常运行还是流量停止后的恢复
}
```

**问题2：停流处理位置错误** (`Flowtrigger.c:496-521`)
```c
else if (!g_ft_scheduler.cycle_started) {
    // 【启动阶段3：等待周期起点】
    if (calendar.hour == g_ft_scheduler.cycle_start_hour && calendar.min == 0) {
        // ...
    }
    return;  // ★ 问题：在等待整点阶段直接返回，后面的停流处理无法执行
}

// === 流量停止处理 ===  ← 这段代码在等待整点阶段永远不会执行
if (g_ft_scheduler.flow_stopped && !g_ft_scheduler.waiting_delivery_complete) {
    // ...
}
```

---

## 2. 设计目标

### 2.1 目标行为定义

```
┌─────────────────────────────────────────────────────────────────┐
│                      流量触发模式简化流程                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  流量上升沿 ──→ 瞬时送样 ──→ 满量采样(A桶) ──→ 等待整点          │
│                                                    │            │
│                                                    ↓            │
│                                            周期采样/送样         │
│                                           (时间等比模式)         │
│                                                    │            │
│  ┌─────────────────────────────────────────────────┘            │
│  │                                                              │
│  │  流量下降沿（任意阶段）                                        │
│  │       │                                                      │
│  │       ↓                                                      │
│  │  立即停止所有操作 ──→ 排空AB桶 ──→ 待机(IDLE)                  │
│  │                                        │                     │
│  │                                        ↓                     │
│  └──────────────────────── 下次流量上升沿 ←┘                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心约束

1. **停流优先级最高**：任何阶段收到停流信号，立即停止，不等待任何操作完成
2. **停流后必须可重触发**：停流回到IDLE后，下次流量触发必须重新执行完整启动链
3. **不允许"已在运行，忽略触发"**：停流后再来流必须响应
4. **简化留样逻辑**：流量模式下停流直接排空，不执行留样判定

### 2.3 重构范围

- **修改范围**：仅流量触发模式（`SamplingMode == 3`）
- **不修改**：时间等比、定时、开关、通讯模式
- **复用逻辑**：周期采样/送样触发点计算（复用时间等比模式）

---

## 3. 目标状态机设计

### 3.1 状态定义

```c
typedef enum {
    FT_IDLE = 0,              // 待机状态
    FT_START_INSTANT,         // 瞬时送样阶段
    FT_START_FULL_SAMPLE,     // 满量采样阶段
    FT_WAIT_HOUR,             // 等待周期整点
    FT_CYCLE_RUNNING,         // 周期运行（采样/送样）
    FT_FORCE_STOP_DRAIN       // 强制停机排空（过渡态）
} FlowTriggerState;
```

### 3.2 事件定义

```c
typedef enum {
    FT_EVT_NONE        = 0,
    FT_EVT_FLOW_START  = (1 << 0),  // 流量上升沿
    FT_EVT_FLOW_STOP   = (1 << 1),  // 流量下降沿
} FlowTriggerEventBits;
```

### 3.3 状态迁移图

```
                    EV_FLOW_START
        ┌──────────────────────────────────┐
        │                                  │
        ↓                                  │
   ┌─────────┐                             │
   │ FT_IDLE │←────────────────────────────┼───────────────┐
   └────┬────┘                             │               │
        │ EV_FLOW_START                    │               │
        ↓                                  │               │
   ┌─────────────────┐    EV_FLOW_STOP     │               │
   │ FT_START_INSTANT│─────────────────────┼──→┐           │
   └────────┬────────┘                     │   │           │
            │ 完成                          │   │           │
            ↓                              │   │           │
   ┌─────────────────────┐  EV_FLOW_STOP   │   │           │
   │ FT_START_FULL_SAMPLE│─────────────────┼──→┤           │
   └──────────┬──────────┘                 │   │           │
              │ 完成                        │   │           │
              ↓                            │   │           │
   ┌─────────────┐        EV_FLOW_STOP     │   │           │
   │ FT_WAIT_HOUR│─────────────────────────┼──→┤           │
   └──────┬──────┘                         │   │           │
          │ 到达整点                        │   │           │
          ↓                                │   │           │
   ┌─────────────────┐    EV_FLOW_STOP     │   │           │
   │ FT_CYCLE_RUNNING│─────────────────────┼──→┤           │
   └─────────────────┘                     │   │           │
                                           │   ↓           │
                                           │ ┌───────────────────┐
                                           │ │ FT_FORCE_STOP_DRAIN│
                                           │ └─────────┬─────────┘
                                           │           │ 排空完成
                                           │           ↓
                                           └───────────┘
```

---

## 4. 数据结构改造

### 4.1 简化后的状态结构

**文件**：`middlewares/bsp/Flowtrigger.h`

```c
// ==================== 删除的字段 ====================
// flow_stopped              - 不再需要，由状态机管理
// waiting_delivery_complete - 不再需要，停流立即停止
// retention_notified        - 不再需要，不通知留样
// flow_stopped_delivery_bucket - 不再需要
// sampling_stopped          - 不再需要
// flow_stop_time            - 可选保留用于日志

// ==================== 新增的字段 ====================
typedef struct {
    // === 状态机核心 ===
    FlowTriggerState state;           // 当前状态
    uint32_t pending_events;          // 待处理事件位

    // === 基本信息 ===
    uint8_t is_initialized;
    uint32_t flow_start_time;         // 流量开始时间戳

    // === 启动阶段 ===
    uint32_t instant_delivery_end_time;

    // === 周期阶段 ===
    uint8_t cycle_start_hour;         // 周期起点小时
    uint32_t cycle_idx;               // 当前周期索引
    uint8_t active_bucket;            // 当前活跃桶 (0=A, 1=B)
    uint32_t sample_done_mask;        // 采样完成掩码
    uint8_t sample_count;             // 每周期采样次数
    uint16_t sample_offsets[24];      // 采样时间点数组
    uint8_t delivery_done;            // 本周期送样完成标志
    uint8_t configured_delivery_min;  // 配置的送样分钟

    // === 首周期延迟（保留） ===
    uint8_t first_cycle_delayed;
    uint16_t delay_offset_min;

    // === 统计信息 ===
    uint32_t total_cycles;
    uint32_t total_samples;
    uint32_t total_deliveries;

    // === 排空控制 ===
    uint32_t drain_start_time;        // 排空开始时间
    uint8_t drain_in_progress;        // 排空进行中标志
} FlowTriggerSchedulerState;
```

### 4.2 新增接口声明

**文件**：`middlewares/bsp/Flowtrigger.h`

```c
// === 状态查询接口 ===
uint8_t ft_scheduler_is_idle(void);
uint8_t ft_scheduler_get_state(void);

// === 强制停机接口 ===
void ft_scheduler_force_stop_and_drain(void);

// === 废弃接口（保留空实现，下版本删除） ===
void flow_trigger_handle_delivery_complete(uint8_t bucket_id, uint16_t delivered_volume);
void flow_trigger_retention_complete_callback(void);
```

---

## 5. 逐文件改造清单

### 5.1 `middlewares/bsp/Flowtrigger.c` 改造

#### 5.1.1 删除的代码段

| 行号范围 | 功能描述 | 删除原因 |
|----------|----------|----------|
| 523-633 | 流量停止后等待送样完成逻辑 | 简化为立即停止 |
| 896-916 | `flow_trigger_handle_delivery_complete()` 实现 | 改为空实现 |
| 962-970 | `flow_trigger_retention_complete_callback()` 实现 | 改为空实现 |
| 987+ | `notify_retention_task_flow_stopped()` | 移除调用 |

#### 5.1.2 修改的函数

**A. `ft_scheduler_init()` (行189)**

```c
void ft_scheduler_init(void) {
    memset(&g_ft_scheduler, 0, sizeof(g_ft_scheduler));
    g_ft_scheduler.state = FT_IDLE;
    g_ft_scheduler.is_initialized = 1;
    printf("[流量调度] 初始化完成，状态=IDLE\r\n");
}
```

**B. `ft_scheduler_start()` (行203)**

```c
void ft_scheduler_start(void) {
    // 清除中止标志
    g_manual_operation_abort_flag = 0;

    // 重置所有阶段状态
    g_ft_scheduler.state = FT_START_INSTANT;
    g_ft_scheduler.flow_start_time = rtc_counter_get();
    g_ft_scheduler.active_bucket = 0;  // 从A桶开始
    g_ft_scheduler.cycle_idx = 0;
    g_ft_scheduler.sample_done_mask = 0;
    g_ft_scheduler.delivery_done = 0;
    g_ft_scheduler.drain_in_progress = 0;

    printf("[流量调度] 调度器已启动，状态=START_INSTANT\r\n");
}
```

**C. `ft_scheduler_stop()` (行251)**

```c
void ft_scheduler_stop(void) {
    g_ft_scheduler.state = FT_IDLE;
    g_ft_scheduler.pending_events = 0;
    g_ft_scheduler.drain_in_progress = 0;
    printf("[流量调度] 调度器已停止，状态=IDLE\r\n");
}
```

**D. `handle_flow_trigger_notifications()` (行341) - 重写**

```c
static void handle_flow_trigger_notifications(void) {
    // 原子读取并清除事件标志
    taskENTER_CRITICAL();
    uint32_t events = g_ft_notify_flags;
    g_ft_notify_flags = 0;
    taskEXIT_CRITICAL();

    // 将事件合并到待处理事件
    g_ft_scheduler.pending_events |= events;

    // 仅记录日志，不在此处处理状态转换
    if (events & FT_NOTIFY_FLOW_START) {
        rtc_time_get();
        printf("[%02d:%02d:%02d][流量调度] 收到流量开始事件\r\n",
               calendar.hour, calendar.min, calendar.sec);
    }
    if (events & FT_NOTIFY_FLOW_STOP) {
        rtc_time_get();
        printf("[%02d:%02d:%02d][流量调度] 收到流量停止事件\r\n",
               calendar.hour, calendar.min, calendar.sec);
    }
}
```

**E. `scheduler_flow_trigger()` (行413) - 完全重写**

```c
void scheduler_flow_trigger(void) {
    // 1. 收集事件
    handle_flow_trigger_notifications();

    // 2. 读取并消费事件（原子操作）
    taskENTER_CRITICAL();
    uint32_t events = g_ft_scheduler.pending_events;
    g_ft_scheduler.pending_events = 0;
    taskEXIT_CRITICAL();

    // 3. 停流事件优先处理（任何非IDLE状态）
    if ((events & FT_EVT_FLOW_STOP) && g_ft_scheduler.state != FT_IDLE) {
        printf("[流量调度] 收到停流事件，进入强制停机\r\n");
        g_ft_scheduler.state = FT_FORCE_STOP_DRAIN;
    }

    // 4. 状态机主循环
    switch (g_ft_scheduler.state) {
        case FT_IDLE:
            ft_process_idle(events);
            break;
        case FT_START_INSTANT:
            ft_process_start_instant();
            break;
        case FT_START_FULL_SAMPLE:
            ft_process_start_full_sample();
            break;
        case FT_WAIT_HOUR:
            ft_process_wait_hour();
            break;
        case FT_CYCLE_RUNNING:
            ft_process_cycle_running();
            break;
        case FT_FORCE_STOP_DRAIN:
            ft_process_force_stop_drain();
            break;
        default:
            g_ft_scheduler.state = FT_IDLE;
            break;
    }
}
```

#### 5.1.3 新增的内部函数

**A. `ft_process_idle()` - IDLE状态处理**

```c
static void ft_process_idle(uint32_t events) {
    if (events & FT_EVT_FLOW_START) {
        rtc_time_get();
        printf("[%02d:%02d:%02d][流量调度] ========== 检测到流量开始 ==========\r\n",
               calendar.hour, calendar.min, calendar.sec);

        // 启动流量触发序列
        ft_scheduler_start();

        // 启动瞬时送样
        printf("[流量调度] 步骤1：瞬时送样启动中...\r\n");
        if (!instant_delivery_start()) {
            printf("[流量调度] 瞬时送样启动失败\r\n");
            ft_scheduler_stop();
        }
    }
}
```

**B. `ft_process_start_instant()` - 瞬时送样阶段**

```c
static void ft_process_start_instant(void) {
    uint8_t status = instant_delivery_update();

    if (status == 1) {  // 完成
        g_ft_scheduler.instant_delivery_end_time = rtc_counter_get();
        g_ft_scheduler.total_deliveries++;
        instant_delivery_reset();

        // 进入满量采样阶段
        g_ft_scheduler.state = FT_START_FULL_SAMPLE;
        printf("[流量调度] 步骤2：满量采样启动中（桶A）...\r\n");

        if (!sampling_start_full_volume(0, 0)) {
            printf("[流量调度] 满量采样启动失败\r\n");
            ft_scheduler_stop();
        }
    } else if (status == 2) {  // 中止
        printf("[流量调度] 瞬时送样已中止\r\n");
        instant_delivery_reset();
        ft_scheduler_stop();
    }
}
```

**C. `ft_process_start_full_sample()` - 满量采样阶段**

```c
static void ft_process_start_full_sample(void) {
    uint8_t samp_status = sampling_get_status();

    if (samp_status == 0 || samp_status == 2) {  // 完成
        // 计算周期起点
        uint8_t cycle_start_hour = calculate_cycle_start_hour(
            g_ft_scheduler.instant_delivery_end_time
        );
        g_ft_scheduler.cycle_start_hour = cycle_start_hour;

        // 初始化周期采样参数
        ft_compute_sample_offsets();

        // 进入等待整点阶段
        g_ft_scheduler.state = FT_WAIT_HOUR;
        printf("[流量调度] 满量采样完成，等待周期起点 %02d:00\r\n", cycle_start_hour);

    } else if (samp_status == 3 || samp_status == 4) {  // 失败/中止
        printf("[流量调度] 满量采样失败/中止\r\n");
        ft_scheduler_stop();
    }
}
```

**D. `ft_process_wait_hour()` - 等待整点阶段**

```c
static void ft_process_wait_hour(void) {
    rtc_time_get();

    if (calendar.hour == g_ft_scheduler.cycle_start_hour && calendar.min == 0) {
        // 进入周期运行阶段
        g_ft_scheduler.state = FT_CYCLE_RUNNING;
        g_ft_scheduler.cycle_idx = 0;
        g_ft_scheduler.active_bucket = 1;  // B桶（A桶已完成满量采样）
        g_ft_scheduler.sample_done_mask = 0;
        g_ft_scheduler.delivery_done = 0;

        printf("[流量调度] 周期开始，活跃桶=B\r\n");
    }
}
```

**E. `ft_process_cycle_running()` - 周期运行阶段**

```c
static void ft_process_cycle_running(void) {
    rtc_time_get();

    // 周期计算（复用时间等比逻辑）
    int16_t hours_since_start = calendar.hour - g_ft_scheduler.cycle_start_hour;
    if (hours_since_start < 0) hours_since_start += 24;

    uint16_t minutes_since_start = hours_since_start * 60 + calendar.min;
    uint16_t cycle_time = g_SampleConfig.CycleTime;
    uint32_t cycle_idx = minutes_since_start / cycle_time;
    uint16_t cycle_off_min = minutes_since_start % cycle_time;

    // 周期切换检测
    if (cycle_idx != g_ft_scheduler.cycle_idx) {
        ft_handle_new_cycle();
        g_ft_scheduler.cycle_idx = cycle_idx;
    }

    // 采样触发检测
    if (calendar.sec == 0) {
        int8_t sample_idx = ft_check_sample_trigger(cycle_off_min);
        if (sample_idx >= 0) {
            uint32_t mask = (1u << sample_idx);
            if ((g_ft_scheduler.sample_done_mask & mask) == 0) {
                uint8_t bucket = g_ft_scheduler.active_bucket;
                if (sampling_start(bucket, g_SampleConfig.SampleVolume, 0, 0)) {
                    g_ft_scheduler.sample_done_mask |= mask;
                    g_ft_scheduler.total_samples++;
                }
            }
        }

        // 送样触发检测
        ft_check_cycle_delivery_trigger(cycle_idx);
    }
}
```

**F. `ft_process_force_stop_drain()` - 强制停机排空阶段**

```c
static void ft_process_force_stop_drain(void) {
    if (!g_ft_scheduler.drain_in_progress) {
        // 首次进入：执行停机操作
        printf("[流量调度] ========== 执行强制停机 ==========\r\n");

        // 1. 设置中止标志
        g_manual_operation_abort_flag = 1;

        // 2. 强制中止瞬时送样
        instant_delivery_reset();

        // 3. 强制中止采样/送样
        sampling_force_abort_if_active();
        delivery_force_abort_if_active();

        // 4. 停止电机
        MotorStop(1);
        MotorStop(2);
        g_State.SampleMotor = 0;
        g_State.DeliveryMotor = 0;

        // 5. 恢复阀门
        InstantThreeWayValveDirect;
        g_State.InstantThreeWayValve = 0;
        SampleThreeWayValveSTAY;
        g_State.SampleThreeWayValve = 0;

        // 6. 启动排空
        printf("[流量调度] 排空AB桶...\r\n");
        DrainARun;
        DrainBRun;

        // 7. 记录排空开始时间
        g_ft_scheduler.drain_start_time = g_tmr2_seconds;
        g_ft_scheduler.drain_in_progress = 1;

        // 8. 记录TSDB事件
        struct {
            uint8_t event_type;
            uint32_t timestamp;
        } ev = { 0x0F, rtc_counter_get() };
        tsdb_event_append(0x80, &ev, sizeof(ev));
    }

    // 检查排空是否完成
    uint16_t drain_duration = g_SampleConfig.BucketDrainTime;
    if (drain_duration == 0 || drain_duration > 600) {
        drain_duration = 60;
    }

    if ((g_tmr2_seconds - g_ft_scheduler.drain_start_time) >= drain_duration) {
        // 排空完成
        DrainAStop;
        DrainBStop;

        // 清除中止标志
        g_manual_operation_abort_flag = 0;

        // 回到IDLE
        ft_scheduler_stop();
        printf("[流量调度] 强制停机完成，进入待机状态\r\n");
    }
}
```

**G. 废弃函数改为空实现**

```c
// 保留接口兼容，下版本删除
void flow_trigger_handle_delivery_complete(uint8_t bucket_id, uint16_t delivered_volume) {
    (void)bucket_id;
    (void)delivered_volume;
    // 空实现 - 流量模式不再依赖此回调
}

void flow_trigger_retention_complete_callback(void) {
    // 空实现 - 流量模式不再依赖此回调
}
```

---

### 5.2 `middlewares/bsp/sampling.c` 改造

#### 5.2.1 新增强制中止函数

```c
/**
 * @brief 强制中止采样（如果正在进行）
 */
void sampling_force_abort_if_active(void) {
    if (sampling_get_status() == 1) {
        printf("[采样] 强制中止采样\r\n");
        g_manual_operation_abort_flag = 1;
        // 调用内部中止函数
        _sampling_abort();
    }
}

/**
 * @brief 强制中止送样（如果正在进行）
 */
void delivery_force_abort_if_active(void) {
    if (delivery_get_status() == 1) {
        printf("[送样] 强制中止送样\r\n");
        g_manual_operation_abort_flag = 1;
        // 调用内部中止函数
        _delivery_abort();
    }
}
```

#### 5.2.2 在 `sampling.h` 中声明

```c
void sampling_force_abort_if_active(void);
void delivery_force_abort_if_active(void);
```

---

### 5.3 `project/src/freertos_app.c` 改造

#### 5.3.1 task4通知模型升级（可选）

如果需要支持流量模式强制停机通知task4，可升级为位通知：

```c
// 事件位定义
#define TASK4_EVT_DELIVERY_A       (1 << 0)
#define TASK4_EVT_DELIVERY_B       (1 << 1)
#define TASK4_EVT_FLOW_FORCE_STOP  (1 << 2)

// task4主循环改造
void task04_func(void *pvParameters) {
    for (;;) {
        uint32_t events = 0;
        xTaskNotifyWait(0, 0xFFFFFFFF, &events, portMAX_DELAY);

        // 流量模式强制停机优先处理
        if (events & TASK4_EVT_FLOW_FORCE_STOP) {
            printf("[task4] 收到流量模式强制停机\r\n");
            // 清空留样上下文
            // 不执行留样判定
            continue;
        }

        // 正常送样完成处理
        if (events & TASK4_EVT_DELIVERY_A) {
            // 处理A桶送样完成
        }
        if (events & TASK4_EVT_DELIVERY_B) {
            // 处理B桶送样完成
        }
    }
}
```

> **注意**：由于简化方案中流量模式停流直接排空，不执行留样，此改造为可选项。

---

## 6. 迁移步骤

### 6.1 阶段1：接口铺设

| 步骤 | 文件 | 操作 |
|------|------|------|
| 1.1 | `Flowtrigger.h` | 添加 `FlowTriggerState` 枚举定义 |
| 1.2 | `Flowtrigger.h` | 修改 `FlowTriggerSchedulerState` 结构体 |
| 1.3 | `Flowtrigger.h` | 添加新接口声明 |
| 1.4 | `sampling.h` | 添加强制中止函数声明 |

### 6.2 阶段2：核心状态机重写

| 步骤 | 文件 | 操作 |
|------|------|------|
| 2.1 | `Flowtrigger.c` | 重写 `ft_scheduler_init/start/stop` |
| 2.2 | `Flowtrigger.c` | 重写 `handle_flow_trigger_notifications` |
| 2.3 | `Flowtrigger.c` | 添加各阶段处理函数 |
| 2.4 | `Flowtrigger.c` | 重写 `scheduler_flow_trigger` 主函数 |

### 6.3 阶段3：辅助函数实现

| 步骤 | 文件 | 操作 |
|------|------|------|
| 3.1 | `sampling.c` | 实现 `sampling_force_abort_if_active` |
| 3.2 | `sampling.c` | 实现 `delivery_force_abort_if_active` |

### 6.4 阶段4：清理旧代码

| 步骤 | 文件 | 操作 |
|------|------|------|
| 4.1 | `Flowtrigger.c` | 删除停流等待逻辑（行523-633） |
| 4.2 | `Flowtrigger.c` | 废弃回调函数改空实现 |
| 4.3 | `sampling.c` | 移除对废弃回调的调用 |

### 6.5 阶段5：测试验证

| 步骤 | 操作 |
|------|------|
| 5.1 | 编译验证无错误 |
| 5.2 | 执行验收用例 |
| 5.3 | 添加状态日志观测 |

---

## 7. 验收用例

### 7.1 功能用例

| 用例ID | 场景 | 预期结果 |
|--------|------|----------|
| A | IDLE状态收到流量开始 | 执行瞬时送样→满量采样→等待整点 |
| B | 等待整点后到达整点 | 进入周期运行，按时间等比触发采样/送样 |
| C | 瞬时送样阶段停流 | 立即中止瞬时送样，排空AB桶，回IDLE |
| D | 满量采样阶段停流 | 立即中止采样，排空AB桶，回IDLE |
| E | 等待整点阶段停流 | 立即排空AB桶，回IDLE（**重点覆盖旧bug**） |
| F | 周期运行中停流 | 立即中止采样/送样，排空AB桶，回IDLE |
| G | 停流后再次来流 | 必须重新触发完整启动链，**不能打印"已在运行，忽略触发"** |

### 7.2 稳定性用例

| 用例ID | 场景 | 预期结果 |
|--------|------|----------|
| H | 快速start-stop-start交替 | 状态机不死锁，正确响应 |
| I | 连续多轮启动停止 | AB桶状态一致，无残留上下文 |
| J | 排空过程中再次来流 | 排空完成后响应新的流量触发 |

### 7.3 日志验证点

```
# 正常启动流程应看到：
[流量调度] ========== 检测到流量开始 ==========
[流量调度] 调度器已启动，状态=START_INSTANT
[流量调度] 步骤1：瞬时送样启动中...
[流量调度] 步骤2：满量采样启动中（桶A）...
[流量调度] 满量采样完成，等待周期起点 XX:00
[流量调度] 周期开始，活跃桶=B

# 停流应看到：
[流量调度] 收到流量停止事件
[流量调度] ========== 执行强制停机 ==========
[流量调度] 排空AB桶...
[流量调度] 强制停机完成，进入待机状态
[流量调度] 调度器已停止，状态=IDLE

# 停流后再来流应看到（关键验证点）：
[流量调度] ========== 检测到流量开始 ==========
[流量调度] 调度器已启动，状态=START_INSTANT  ← 必须看到这行
[流量调度] 步骤1：瞬时送样启动中...          ← 必须看到这行
# 不能看到：[流量调度] 已在运行，忽略触发
```

---

## 8. 风险与控制

### 8.1 风险点

| 风险 | 影响 | 概率 |
|------|------|------|
| 强制中止导致TSDB记录不完整 | 事件记录中出现"中止"类型增多 | 中 |
| 排空时间配置不当 | 排空不彻底或等待过长 | 低 |
| 其他模式误入流量停机分支 | 非流量模式被错误停机 | 低 |

### 8.2 控制措施

1. **模式隔离**：所有流量模式逻辑仅在 `SamplingMode == 3` 时执行
2. **保留旧函数壳**：废弃函数保留空实现一个版本，便于定位残留调用
3. **增加状态日志**：关键状态转换点添加日志，便于问题定位
4. **排空时间校验**：排空时间配置范围检查（0-600秒，默认60秒）

---

## 9. 交付标准

### 9.1 代码层

- [ ] 不再依赖 `flow_trigger_retention_complete_callback()` 进行停流收尾
- [ ] 不再出现 `flow_stopped/waiting_delivery_complete` 等复杂状态判断
- [ ] 状态机清晰，单一入口驱动

### 9.2 行为层

- [ ] 停流后 1 个调度周期内完成停机排空并回待机
- [ ] 停流后再来流必须重新执行完整启动链
- [ ] 任何阶段停流都能正确响应

### 9.3 可观测层

- [ ] 再次流量触发必进入"步骤1：瞬时送样启动中"
- [ ] 不再出现"已在运行，忽略触发"（停流后再来流场景）
- [ ] 状态转换有清晰日志

---

## 10. 附录：代码改动汇总

### 10.1 文件改动清单

| 文件 | 改动类型 | 改动量估计 |
|------|----------|------------|
| `Flowtrigger.h` | 修改 | 约50行 |
| `Flowtrigger.c` | 重写 | 约300行 |
| `sampling.h` | 新增 | 约5行 |
| `sampling.c` | 新增 | 约30行 |
| `freertos_app.c` | 可选修改 | 约20行 |

### 10.2 删除的代码行数

| 文件 | 删除行数 | 说明 |
|------|----------|------|
| `Flowtrigger.c` | 约110行 | 停流等待逻辑 |
| `Flowtrigger.c` | 约30行 | 废弃回调实现 |

---

**文档结束**
