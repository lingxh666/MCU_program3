屏幕分发器如果收到消息  5A A5 06 83 00 00 01 81 33
表示进入通道设置页，开始5s一次发送数值，发送的数值是
通道一：
变量508A表示输入电流，应该输入数值是ADC1计算的电流值。
变量508B表示根据输入电流计算的COD值，根据校准电流和校准值计算得出。
变量508C表示校准电流，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。
变量508D表示校准值，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。

通道二：
变量508E表示输入电流，应该输入数值是ADC2计算的电流值。
变量508F表示根据输入电流计算的COD值，根据校准电流和校准值计算得出。
变量5090表示校准电流，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。
变量5091表示校准值，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。

通道三：
变量5092表示输入电流，应该输入数值是ADC3计算的电流值。
变量5093表示根据输入电流计算的COD值，根据校准电流和校准值计算得出。
变量5094表示校准电流，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。
变量5095表示校准值，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。

通道四：
变量5096表示输入电流，应该输入数值是ADC4计算的电流值。
变量5097表示根据输入电流计算的COD值，根据校准电流和校准值计算得出。
变量5098表示校准电流，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。
变量5099表示校准值，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。

通道五：
变量509A表示输入电流，应该输入数值是ADC5计算的电流值。
变量509B表示根据输入电流计算的COD值，根据校准电流和校准值计算得出。
变量509C表示校准电流，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。
变量509D表示校准值，这个值缺省为0，用户可以修改，修改后存入全局变量和KVDB，反写屏幕需要固定的写入全区变量值。

通道六：
变量509E表示流量4-20mA输入电流（MCU发送，0.01mA精度）。
变量509F表示根据校准值计算的流量值（MCU发送，1位小数）。
变量50A0表示流量校准电流，用户可修改，存入g_CommSettingConfig.FlowADLower（0.1mA单位）。
变量50A1表示流量校准流量，用户可修改，存入g_CommSettingConfig.FlowMeterBase（屏幕值缩小10倍）。


